import type { NextPage } from "next";
import Head from "next/head";

const About: NextPage = ({}) => {
  return (
    <>
      <Head>
        <title>Saivamsi Addagada | About</title>
      </Head>
      <main className="flex flex-1 flex-col overflow-hidden hr:flex-row"></main>
    </>
  );
};

export default About;
